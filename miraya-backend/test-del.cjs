const axios = require('axios'); const jwt = require('jsonwebtoken'); require('dotenv').config(); const t = jwt.sign({ userId: 1, role: 'admin' }, process.env.JWT_SECRET, { expiresIn: '1d' }); axios.delete('http://localhost:5000/api/products/9999', { headers: { Authorization: 'Bearer ' + t } }).then(r => console.log('Success:', r.data)).catch(e => console.error(e.response ? e.response.data : e.message));
