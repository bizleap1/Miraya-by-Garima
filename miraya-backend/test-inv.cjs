const axios = require('axios'); const jwt = require('jsonwebtoken'); require('dotenv').config(); const t = jwt.sign({ userId: 1, role: 'admin' }, process.env.JWT_SECRET, { expiresIn: '1d' }); axios.get('http://localhost:5000/api/inventory?limit=200', { headers: { Authorization: 'Bearer ' + t } }).then(r => console.log(r.data)).catch(e => console.error(e.response ? e.response.data : e.message));
